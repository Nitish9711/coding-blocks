Required Headers

#include <ext/pb_ds/assoc_container.hpp>
#include <ext/pb_ds/tree_policy.hpp>
using namespace __gnu_pbds;

//decalartion for set

typedef tree<int, null_type, less<int>, rb_tree_tag,
             tree_order_statistics_node_update>
    ordered_set;

// for multiset
A drawback of using less_equal instead of less is that lower_bound works as upper_bound
typedef tree<int , null_type ,  less_equal<int> , rb_tree_tag , tree_order_statistics_node_update> ordered_multiset;
// we can also use this
typedef tree<pair<I, I>, null_type, less<pair<I, I>>, rb_tree_tag, tree_order_statistics_node_update>pbds;


//extra functionalities other than set
1. find_by_order(k) -> return the iterator to the the kth largest element.
TC - log(n);
2. order_of_key(k) -> return the number of elements in the set which are strictly less than our value
TC - log(n);
if no

set 15 10 4 3 1
find_by_order(3) - 10
order_of_key(10) ->3

// works on 
codechef
hackerank
leetcode
codeforces